{
  "name": "kodi-dev",
  "version": "0.1.1",
  "description": "kodi.dev — Claude Code-native agent orchestrator CLI",
  "license": "MIT",
  "author": "Luis Soares",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luis0ares/kodi-dev.git"
  },
  "homepage": "https://github.com/luis0ares/kodi-dev#readme",
  "bugs": "https://github.com/luis0ares/kodi-dev/issues",
  "packageManager": "pnpm@11.5.0",
  "keywords": [
    "claude-code",
    "agents",
    "orchestrator",
    "cli",
    "tickets",
    "pull-request"
  ],
  "type": "module",
  "bin": {
    "kodi": "dist/index.js"
  },
  "files": [
    "dist",
    "assets"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "pnpm test:cli && pnpm test:board",
    "test:cli": "vitest run",
    "test:board": "sh -c 'if [ -f board/package.json ]; then pnpm -C board test; else echo \"test:board skipped (board/ not present)\"; fi'",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "pnpm lint:cli && pnpm lint:board",
    "lint:cli": "eslint src tests && prettier --check .",
    "lint:board": "sh -c 'if [ -f board/package.json ]; then pnpm -C board lint; else echo \"lint:board skipped (board/ not present)\"; fi'",
    "format": "prettier --write .",
    "prepublishOnly": "tsup"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.2.1",
    "commander": "^12.1.0",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "eslint": "^10.6.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.4",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.63.0",
    "vitest": "^2.1.2"
  }
}
