{
  "name": "kodi-dev",
  "version": "1.3.0",
  "description": "kodi.dev — Claude Code-native agent orchestrator CLI",
  "license": "MIT",
  "author": "Luis Soares",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luis0ares/kodi-dev.git"
  },
  "homepage": "https://github.com/luis0ares/kodi-dev#readme",
  "bugs": "https://github.com/luis0ares/kodi-dev/issues",
  "packageManager": "pnpm@11.5.0",
  "keywords": [
    "claude-code",
    "agents",
    "orchestrator",
    "cli",
    "tickets",
    "pull-request"
  ],
  "type": "module",
  "bin": {
    "kodi": "dist/index.js"
  },
  "files": [
    "dist",
    "assets",
    "board-dist"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm build:board && tsup",
    "build:board": "pnpm -C board install --prefer-offline --node-linker=hoisted && pnpm -C board build && node scripts/build-board.mjs",
    "dev": "tsup --watch",
    "test": "pnpm test:cli && pnpm test:board",
    "test:cli": "vitest run",
    "test:board": "sh -c 'if [ -f board/package.json ]; then pnpm -C board test; else echo \"test:board skipped (board/ not present)\"; fi'",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "pnpm lint:cli && pnpm lint:board",
    "lint:cli": "eslint src tests && prettier --check .",
    "lint:board": "sh -c 'if [ -f board/package.json ]; then pnpm -C board lint; else echo \"lint:board skipped (board/ not present)\"; fi'",
    "format": "prettier --write .",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.2.1",
    "commander": "^12.1.0",
    "cross-spawn": "^7.0.6",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^22.7.4",
    "eslint": "^10.6.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.4",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.63.0",
    "vitest": "^2.1.2"
  }
}
